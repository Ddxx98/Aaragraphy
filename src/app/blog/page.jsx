import React from 'react';
import Blogs from '../../views/Blogs/Blogs';

export const metadata = {
  title: 'Stories & Blog | Aaragraphy Photography',
  description: 'Browse our latest photography stories, client sessions, engagement shoots, and weddings captured across Ireland.',
};

export default function BlogPage() {
  return <Blogs />;
}
